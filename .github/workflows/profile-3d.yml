name: GitHub-Profile-3D-Contrib

on:
  schedule: # 03:00 JST == 18:00 UTC
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-github-profile-3d-contrib
    steps:
      - uses: actions/checkout@v3
      - uses: kanaji2002/github-profile-3d-contrib@0.7.1
        env:
          GITHUB_TOKEN: ${{ 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 }}
          USERNAME: ${{ kanaji2002 }}
      - name: Commit & Push
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add -A .
          git commit -m "generated"
          git push
